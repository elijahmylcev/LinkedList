describe('push(Node):', () => {
    test.todo('inserts an element at the end of the list')
    test.todo('should return boolean')
});

describe('insert(Node, index):', () => {
    test.todo('inserts element at index')
    test.todo('should return boolean')
});

describe('get(index):', () => {
    test.todo('should return the element at the specified index')
    test.todo('should return null if there is no element with an index')
});

describe('length:', () => {
    test.todo('getter, returns the actual length of the list')
});
